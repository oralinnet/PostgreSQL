--- Run sql command on other user with password 
vim $PGDATA/pg_hba.conf
local   all             all                                     scram-sha-256

/usr/pgsql-15/bin/pg_ctl -D $PGDATA reload

psql postgresql://postgres:Uat#ABGTLdb23@10.11.245.12/postgres <<EOF
SELECT pg_database.datname as "database_name", pg_database_size(pg_database.datname)/1024/1024 AS size_in_mb FROM pg_database ORDER by size_in_mb DESC;
EOF